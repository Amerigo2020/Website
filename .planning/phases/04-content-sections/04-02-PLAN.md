---
phase: 04-content-sections
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - client/src/index.php
  - client/src/assets/css/app.css
autonomous: true

must_haves:
  truths:
    - "The #services section uses .capabilities list layout — no .services__grid, no .service-card, no emoji icons"
    - "Each capability has a monospace .capability-label in accent color and a prose paragraph describing client output"
    - "The services intro paragraph is preserved"
    - "Old service card CSS rules are removed from app.css — no orphaned .services__grid, .service-card, .service-icon, .service-card:hover, .service-card:focus rules remain"
  artifacts:
    - path: "client/src/index.php"
      provides: "Rebuilt #services section with .capabilities list"
      contains: "class=\"capabilities\""
    - path: "client/src/assets/css/app.css"
      provides: "Capabilities list CSS; old service card CSS removed"
      contains: ".capabilities"
  key_links:
    - from: "nav anchor #services"
      to: "section id='services'"
      via: "HTML id attribute preserved"
      pattern: "id=\"services\""
    - from: ".capability-label"
      to: "--font-mono"
      via: "existing JetBrains Mono allowlist at line 106"
      pattern: "\\.capability-label"
---

<objective>
Upgrade the #services section from centered emoji icon cards to a left-aligned .capabilities list with monospace labels. The copy is already correct from Phase 2 — this is a structural upgrade only. Remove all old service card CSS.

Purpose: SEC-03 — the capabilities section must use monospace labels and describe output, not technology. The current layout uses centered card grids with emoji icons, which produces an agency-brochure aesthetic that contradicts the "one person" positioning.

Output: Rebuilt #services HTML with .capabilities/.capability/.capability-label structure. New capability CSS in app.css. Old .services__grid, .service-card, .service-icon, and related rules removed from app.css.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-content-sections/04-RESEARCH.md
@.planning/phases/04-content-sections/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace #services section HTML with capabilities list</name>
  <files>client/src/index.php</files>
  <action>
Replace lines 329–355 (the entire #services section from opening tag to closing </section>) with:

        <section id="services" class="section services">
            <div class="container">
                <h2>What I build</h2>
                <p class="services__intro">You describe a problem. Some weeks later, you have a system that runs: a backend that handles your business logic, a front end your team can actually use, and deployments that don't require you to call me at 2am.</p>

                <div class="capabilities">
                    <div class="capability">
                        <span class="capability-label">AI & Automation</span>
                        <p>I automate the workflows your team wastes hours on. The result: AI-assisted processes that run without manual intervention, integrated into the systems you already use.</p>
                    </div>

                    <div class="capability">
                        <span class="capability-label">Web Applications & Hosting</span>
                        <p>A complete web presence: fast, accessible, and maintained. I handle the domain, the hosting, the deployment pipeline, and the monitoring — so the site stays up and you stay focused on your business.</p>
                    </div>

                    <div class="capability">
                        <span class="capability-label">DevOps & Deployment</span>
                        <p>I set up the pipelines that let you ship without fear. CI/CD, infrastructure as code, observability — so every release is predictable and every incident is visible.</p>
                    </div>
                </div>

            </div>
        </section>

Rules:
- The section id MUST remain "services" — the nav anchor #services points here.
- The class remains "section services" — the .services CSS rule (background: --bg-surface) is unchanged.
- .capability-label is already in the JetBrains Mono allowlist in app.css (line 106-113) — no font-family rule needed on the element itself.
- Copy for each capability paragraph is preserved exactly from the original service card copy from Phase 2 — do not alter it.
- The services intro paragraph copy is preserved exactly from the original.
- No tabindex="0" on .capability divs — these are not interactive elements.
- No service-icon divs, no emoji, no h3 inside capabilities.
  </action>
  <verify>
grep -n "id=\"services\"" client/src/index.php
grep -n "service-card" client/src/index.php
grep -n "service-icon" client/src/index.php
grep -n "class=\"capabilities\"" client/src/index.php
grep -n "capability-label" client/src/index.php
  </verify>
  <done>
grep for "service-card" returns zero results in index.php. grep for "class=\"capabilities\"" returns exactly one hit. grep for "capability-label" returns exactly three hits (one per capability). grep for "id=\"services\"" returns one hit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace service card CSS with capabilities CSS in app.css</name>
  <files>client/src/assets/css/app.css</files>
  <action>
Make the following targeted changes to app.css:

REMOVE these rule blocks (identified by approximate line numbers — locate by content):
1. ".services__grid { ... }" block (lines ~430–435) — the entire rule
2. ".service-card { ... }" block (lines ~437–444) — the entire rule
3. ".service-card:hover { ... }" block (lines ~446–449) — the entire rule
4. ".service-icon { ... }" block (lines ~451–462) — the entire rule
5. ".service-card h3 { ... }" block (lines ~464–467) — the entire rule
6. ".service-card p { ... }" block (lines ~469–472) — the entire rule
7. The "@media (min-width: 769px) { .services__grid { ... } }" block (lines ~678–682) — the entire rule including the media query wrapper if .services__grid is its only content
8. ".service-card:focus { ... }" block (lines ~712–715) — under the "/* Focus styles for keyboard navigation */" comment

KEEP the ".services { background: var(--bg-surface); }" rule — it is still needed.

After removing the old rules, INSERT the following CSS block immediately after ".services { background: var(--bg-surface); }":

.services__intro {
  color: var(--text-secondary);
  max-width: var(--max-width-prose);
  margin-top: var(--space-4);
}

.capabilities {
  margin-top: var(--space-12);
  display: flex;
  flex-direction: column;
  gap: var(--space-10);
  max-width: var(--max-width-prose);
}

.capability {
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}

.capability p {
  color: var(--text-secondary);
  line-height: 1.7;
  margin-bottom: 0;
}

Rules:
- The .capability-label font-family is already handled by the existing allowlist at line 106 — do NOT add a duplicate font-family rule.
- Use only defined --space-* tokens. --space-10 does not exist — use gap: 2.5rem directly for the capabilities gap (this is the only exception; there is no --space-10 token in :root).
- After removal, verify no orphaned .service-card or .services__grid rules remain anywhere in app.css.
  </action>
  <verify>
grep -n "service-card" client/src/assets/css/app.css
grep -n "services__grid" client/src/assets/css/app.css
grep -n "service-icon" client/src/assets/css/app.css
grep -n "\.capabilities" client/src/assets/css/app.css
grep -n "\.capability-label" client/src/assets/css/app.css
  </verify>
  <done>
grep for "service-card" in app.css returns zero results. grep for "services__grid" returns zero results. grep for "service-icon" returns zero results. grep for ".capabilities" returns the new .capabilities and .capability rules. The existing .capability-label entry in the JetBrains Mono allowlist at line 106 remains untouched.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Section order in index.php: #services → #experience (.about) → #contact. Confirm with: grep -n "id=\"services\"\|id=\"experience\"\|id=\"contact\"\|id=\"proof\"" client/src/index.php
2. No emoji icon circles in #services: grep -n "service-icon\|🤖\|🌐\|⚙️" client/src/index.php should return zero results.
3. No orphaned service card CSS: grep -n "service-card\|services__grid\|service-icon" client/src/assets/css/app.css should return zero results.
4. .capabilities and .capability rules exist: grep -n "\.capabilities\|\.capability " client/src/assets/css/app.css should return new rules.
</verification>

<success_criteria>
- #services section uses .capabilities list structure — no .services__grid, no .service-card, no emoji icons
- Three .capability items each with .capability-label and prose paragraph
- All old service card CSS rules removed from app.css
- .services { background: var(--bg-surface); } preserved
- Nav anchor #services remains functional
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-sections/04-02-SUMMARY.md` using the summary template.
</output>
