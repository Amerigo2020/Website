---
phase: 04-content-sections
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - client/src/index.php
  - client/src/assets/css/app.css
autonomous: true

must_haves:
  truths:
    - "A #proof section exists between the #experience (about) section and the #contact section"
    - "The proof section shows exactly 4 items: Enactus Worldcup Bangkok, MSG Hackathon, EY, Jörg Velletti EDV Service"
    - "Each item has a monospace label and exactly one sentence of context"
    - "The proof section has --bg-surface background (alternating with --bg-base of the About section)"
    - "No chip grids remain anywhere in the page body (chips removed in 04-01, confirmed here)"
  artifacts:
    - path: "client/src/index.php"
      provides: "New #proof section inserted before #contact"
      contains: "id=\"proof\""
    - path: "client/src/assets/css/app.css"
      provides: "Proof section CSS rules"
      contains: ".proof__list"
  key_links:
    - from: "#proof section"
      to: "#contact section"
      via: "section order in HTML — proof appears immediately before contact"
      pattern: "id=\"proof\""
    - from: ".proof-item__label"
      to: "--font-mono"
      via: "proof-item__label class added to JetBrains Mono allowlist"
      pattern: "\\.proof-item__label"
---

<objective>
Insert a new #proof section between the #experience (about) and #contact sections. The section shows exactly 4 curated proof items with monospace labels and one-sentence context each. Add proof CSS to app.css and extend the JetBrains Mono allowlist to include .proof-item__label.

Purpose: SEC-04 — the 14-chip hackathon grid was removed in 04-01. The proof section provides focused, context-rich proof without visual noise. Four items: Enactus Worldcup Bangkok, MSG Hackathon, EY, family business origin.

Output: #proof section in index.php at the correct position. Proof CSS in app.css. .proof-item__label added to the monospace allowlist.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-content-sections/04-RESEARCH.md
@.planning/phases/04-content-sections/04-01-SUMMARY.md
@.planning/phases/04-content-sections/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert #proof section into index.php</name>
  <files>client/src/index.php</files>
  <action>
After the closing </section> of the #experience section (which after 04-01 is a short section ending with </section>) and BEFORE the "<!-- Contact Section -->" comment and opening <section id="contact">, insert:

        <!-- Proof Section -->
        <section id="proof" class="section proof">
            <div class="container">
                <h2>Selected proof</h2>

                <div class="proof__list">
                    <div class="proof-item">
                        <span class="proof-item__label">Enactus Germany Worldcup — Bangkok 2025</span>
                        <p class="proof-item__context">Represented Germany at the international Enactus competition and won — the team built a working product under competition conditions in Bangkok.</p>
                    </div>
                    <div class="proof-item">
                        <span class="proof-item__label">MSG Hackathon — Code & Create</span>
                        <p class="proof-item__context">Placed top 3 at the MSG Code & Create hackathon, shipping a functional prototype within 24 hours against a field of professional developers.</p>
                    </div>
                    <div class="proof-item">
                        <span class="proof-item__label">EY — Working Student, Transfer Pricing</span>
                        <p class="proof-item__context">Currently working at EY Munich in Transfer Pricing, applying analytical systems thinking in a Big Four advisory environment.</p>
                    </div>
                    <div class="proof-item">
                        <span class="proof-item__label">Jörg Velletti EDV Service</span>
                        <p class="proof-item__context">Grew up maintaining production systems at the family IT business — which meant real accountability for real clients before starting university.</p>
                    </div>
                </div>
            </div>
        </section>

Rules:
- Section id must be "proof" — this is a new id, no nav anchor currently points to it.
- Insert BEFORE "<!-- Contact Section -->" comment, AFTER the #experience section's closing </section>.
- Do not edit the #contact section — it starts immediately after the inserted block.
- No inline style="" attributes.
- Each proof-item__context is exactly one sentence ending with a period.
- proof-item__label uses JetBrains Mono via the allowlist (added in Task 2) — no inline style needed.
  </action>
  <verify>
grep -n "id=\"proof\"" client/src/index.php
grep -n "proof-item" client/src/index.php
grep -c "class=\"proof-item\"" client/src/index.php
grep -n "id=\"contact\"" client/src/index.php
  </verify>
  <done>
grep for "id=\"proof\"" returns exactly one hit. grep -c for class="proof-item" returns 4 (one per proof item). grep for "id=\"contact\"" confirms the contact section still exists after the proof section. Section order in index.php is: #services → #experience → #proof → #contact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add proof CSS to app.css and extend monospace allowlist</name>
  <files>client/src/assets/css/app.css</files>
  <action>
Make two changes to app.css:

CHANGE 1 — Extend the JetBrains Mono allowlist at lines 106–113. The current block is:
  .eyebrow,
  .capability-label,
  .chip--date,
  .logo,
  pre,
  code {
    font-family: var(--font-mono);
  }

Add ".proof-item__label," after ".capability-label," so it becomes:
  .eyebrow,
  .capability-label,
  .proof-item__label,
  .chip--date,
  .logo,
  pre,
  code {
    font-family: var(--font-mono);
  }

CHANGE 2 — Insert the proof section CSS. Locate the "/* Contact Section */" comment (around line 474). Insert the following block immediately BEFORE that comment:

/* Proof Section */
.proof {
  background: var(--bg-surface);
}

.proof__list {
  margin-top: var(--space-12);
  display: flex;
  flex-direction: column;
  max-width: var(--max-width-prose);
}

.proof-item {
  padding: var(--space-6) 0;
  border-bottom: 1px solid var(--border-subtle);
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
}

.proof-item:first-child {
  border-top: 1px solid var(--border-subtle);
}

.proof-item__label {
  font-size: var(--text-sm);
  color: var(--text-primary);
  letter-spacing: 0.03em;
}

.proof-item__context {
  color: var(--text-secondary);
  margin-bottom: 0;
  font-size: var(--text-base);
  line-height: 1.6;
}

Rules:
- .proof background is --bg-surface (#111111) — alternates with .about's --bg-base (#0a0a0a).
- .proof-item__label color is --text-primary (not --accent) — these are factual labels, not decorative identifiers like capability-label.
- --max-width-prose is already defined in :root — use it.
- Use only defined --space-* tokens: --space-2, --space-6, --space-12.
- The border-bottom list pattern requires no `gap` on .proof__list — spacing is handled by padding on .proof-item.
  </action>
  <verify>
grep -n "proof-item__label" client/src/assets/css/app.css
grep -n "\.proof" client/src/assets/css/app.css
grep -n "proof__list" client/src/assets/css/app.css
grep -A 10 "font-family: var(--font-mono)" client/src/assets/css/app.css | head -20
  </verify>
  <done>
grep for "proof-item__label" in app.css returns two hits: one in the JetBrains Mono allowlist block and one in the .proof-item__label rule. grep for ".proof" returns the new .proof, .proof__list, .proof-item, .proof-item:first-child, .proof-item__label, .proof-item__context rules. The monospace allowlist now includes .proof-item__label.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Page section order: grep -n "id=\"services\"\|id=\"experience\"\|id=\"proof\"\|id=\"contact\"" client/src/index.php — should appear in that order.
2. Exactly 4 proof items: grep -c "class=\"proof-item\"" client/src/index.php should return 4.
3. No chip grids anywhere in the body sections: grep -n "cards-grid\|event-card" client/src/index.php — these may appear in the legal sections (Impressum) but should NOT appear between #services and #contact.
4. Visual background alternation logic: services=--bg-surface, about=--bg-base, proof=--bg-surface, contact=--bg-base.
5. Confirm Impressum section at line 614+ is untouched: the file lines after #contact should be identical to their state before this plan.
</verification>

<success_criteria>
- #proof section exists between #experience and #contact in index.php
- Exactly 4 proof items with monospace labels and one-sentence context paragraphs
- .proof background is --bg-surface (visual alternation correct)
- .proof-item__label renders in JetBrains Mono via the updated allowlist
- Impressum section (line 614+) is untouched
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-sections/04-03-SUMMARY.md` using the summary template.
</output>
