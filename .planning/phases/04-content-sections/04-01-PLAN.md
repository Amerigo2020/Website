---
phase: 04-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/index.php
  - client/src/assets/css/app.css
autonomous: true

must_haves:
  truths:
    - "The #experience section contains only an h2 heading and a single prose paragraph — no chip grid, no cards, no h3 subheadings, no LinkedIn/GitHub cards"
    - "The origin story paragraph is preserved word-for-word from Phase 2"
    - "Nav anchor #experience still scrolls to the About section without breaking"
    - "No inline style attributes remain on the rebuilt section or its children"
  artifacts:
    - path: "client/src/index.php"
      provides: "Rebuilt #experience section as story-only prose"
      contains: "class=\"section about\""
    - path: "client/src/assets/css/app.css"
      provides: "Story section CSS rules"
      contains: ".about__prose"
  key_links:
    - from: "nav anchor #experience"
      to: "section id='experience'"
      via: "HTML id attribute preserved"
      pattern: "id=\"experience\""
    - from: ".about__prose"
      to: "--max-width-prose"
      via: "CSS custom property"
      pattern: "max-width: var\\(--max-width-prose\\)"
---

<objective>
Rebuild the #experience section as a focused story section. Strip all chip grids, card lists, h3 subheadings, and LinkedIn/GitHub profile cards. Preserve the origin story paragraph. Apply .about class and add prose CSS.

Purpose: SEC-02 — the story/about section must read as a continuous first-person narrative connecting the family business, TUM, and present capability. The current section mixes 14 event chips, education cards, industry cards, and network cards with the narrative paragraph — this makes the section unreadable as a story.

Output: A clean #experience section with h2 "About" and a single .about__prose container holding the origin story. CSS rules for .about and .about__prose in app.css.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-sections/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace #experience section in index.php with story-only prose</name>
  <files>client/src/index.php</files>
  <action>
Replace lines 357–579 (the entire <!-- Experience / Events Section --> comment through the closing </section>) with the following:

        <!-- About / Story Section -->
        <section id="experience" class="section about">
            <div class="container">
                <h2>About</h2>
                <div class="about__prose">
                    <p>I grew up helping run my family's IT services company, Jörg Velletti EDV Service — which meant debugging production systems long before I enrolled at university. At TUM studying Business Informatics, I developed the systems-thinking framing that connects technical decisions to business outcomes. Today I work as a student at EY in Transfer Pricing and compete in hackathons to keep shipping under pressure. I've won at the Enactus Germany Worldcup (Bangkok 2025) and placed top 3 at the MSG Hackathon.</p>
                </div>
            </div>
        </section>

Rules:
- The section id MUST remain "experience" — the nav anchor #experience points here.
- Change only the class: from "section experience" to "section about".
- The origin story paragraph text is preserved exactly from line 361 — do not paraphrase or alter it.
- Remove everything from line 363 (the cards-grid div with LinkedIn card) through line 578 (the closing div of the last cards-grid).
- No inline style="" attributes on any element.
- The LinkedIn/GitHub profile cards are removed entirely — they will be added to the footer in Phase 5, not here.
- Education cards, Industry Experience cards, Networks cards are permanently removed — they are not going elsewhere.
- The h3 subheadings ("Education", "Industry Experience", "Networks & Memberships") are removed.
- The 14 event-card chips are removed.
  </action>
  <verify>
grep -n "id=\"experience\"" client/src/index.php
grep -n "cards-grid" client/src/index.php
grep -n "event-card" client/src/index.php
grep -n "profile-card" client/src/index.php
grep -n "class=\"section about\"" client/src/index.php
  </verify>
  <done>
grep for "cards-grid" returns zero results inside the #experience section. grep for "id=\"experience\"" returns exactly one hit. grep for "class=\"section about\"" returns exactly one hit. The section contains only h2, .about__prose, and the origin story paragraph.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add story section CSS to app.css</name>
  <files>client/src/assets/css/app.css</files>
  <action>
Locate the line in app.css that reads "/* Services Section */" (around line 425). Insert the following CSS block immediately BEFORE that comment — i.e., between the .btn--ghost:hover/.btn--ghost:focus rule block and the /* Services Section */ comment:

/* Story / About Section */
.about {
  background: var(--bg-base);
}

.about__prose {
  max-width: var(--max-width-prose);
  margin-top: var(--space-8);
}

.about__prose p {
  color: var(--text-secondary);
  font-size: var(--text-lg);
  line-height: 1.8;
  margin-bottom: var(--space-6);
}

Rules:
- Use only tokens already defined in :root — no hardcoded pixel values.
- --bg-base is #0a0a0a — the story section background alternates with #services (--bg-surface).
- --max-width-prose is 680px — already defined at line 54 of app.css.
- Do not remove any existing CSS — only insert the new block.
  </action>
  <verify>
grep -n "\.about" client/src/assets/css/app.css
grep -n "\.about__prose" client/src/assets/css/app.css
grep -n "max-width: var(--max-width-prose)" client/src/assets/css/app.css
  </verify>
  <done>
grep for ".about" returns the new .about, .about__prose, and .about__prose p rules. grep for "max-width: var(--max-width-prose)" returns at least one hit. No existing rules were removed.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Open index.php and visually confirm lines around the former #experience section — it should be ~8 lines, not ~220.
2. Confirm the section order in index.php: #services → #experience (now .about) → #contact.
3. Confirm no inline style= attributes remain on the rebuilt section.
4. Confirm the nav anchor still points correctly: grep -n "href=\"#experience\"" client/src/index.php should return both the desktop and mobile nav entries.
5. In app.css, confirm .about rules exist and .experience class no longer has any rules (it was never a CSS class, so no cleanup needed there).
</verification>

<success_criteria>
- #experience section is ~8 lines: section open, container, h2, about__prose div, p (origin story), close divs, section close
- No event-card, cards-grid, profile-card, or h3 subheadings remain in #experience
- .about and .about__prose CSS rules exist in app.css using only defined tokens
- Nav anchors #experience remain functional (id preserved on section)
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-sections/04-01-SUMMARY.md` using the summary template.
</output>
