---
phase: 03-navigation-and-layout-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/index.php
  - client/src/assets/css/app.css
autonomous: true

must_haves:
  truths:
    - "Nav shows 'About', 'What I build', 'Contact' — in that order left-to-right"
    - "Desktop nav href targets are #experience, #services, #contact respectively"
    - "Mobile menu shows identical labels and closes on link tap"
    - "No Impressum or Datenschutz link exists anywhere inside the <header> element"
    - "Impressum and Datenschutz links exist in the <footer> element"
    - "Footer background is --bg-base (not --bg-surface)"
  artifacts:
    - path: "client/src/index.php"
      provides: "Updated desktop nav (lines 254–258) and mobile nav (lines 287–291)"
      contains: "About</a>"
    - path: "client/src/assets/css/app.css"
      provides: "Footer background fix"
      contains: "background: var(--bg-base)"
  key_links:
    - from: "desktop nav <a href='#experience'>"
      to: "<section id='experience'>"
      via: "anchor scroll"
      pattern: "href=\"#experience\""
    - from: "mobile nav <a href='#experience'>"
      to: "<section id='experience'>"
      via: "anchor scroll + closeMobileMenu()"
      pattern: "closeMobileMenu.*About|About.*closeMobileMenu"
---

<objective>
Update nav link labels to match actual section content; verify Impressum/Datenschutz are footer-only; fix footer background token.

Purpose: NAV-01 — labels must reflect redesigned section structure. NAV-02 — legal links in footer only (already true; this plan verifies and documents). Footer background fix prevents visual inconsistency when contact section (--bg-base) bleeds into footer (currently --bg-surface).
Output: index.php with correct nav labels in both desktop and mobile nav; app.css with footer background on --bg-base.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-and-layout-primitives/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update desktop and mobile nav labels in index.php</name>
  <files>client/src/index.php</files>
  <action>
    VERIFY FIRST (do not skip): Run `grep -n "impressum\|privacy\|Impressum\|Datenschutz" client/src/index.php | grep -E "^(24[89]|25[0-9]|26[0-9]|27[0-9]|28[0-9]|29[0-4]):"` — the result must be empty (zero matches in lines 248–294). If any match is found, stop and report before making any changes.

    Make the following two targeted replacements in index.php:

    REPLACEMENT A — Desktop nav (lines 254–258). Replace the entire `<nav class="nav" role="navigation" aria-label="Main navigation">` block with:
    ```html
    <nav class="nav" role="navigation" aria-label="Main navigation">
        <a href="#experience" class="nav__link">About</a>
        <a href="#services" class="nav__link">What I build</a>
        <a href="#contact" class="nav__link">Contact</a>
    </nav>
    ```

    REPLACEMENT B — Mobile nav (lines 287–291). Replace the entire `<nav class="nav" role="navigation" aria-label="Mobile navigation">` block inside `<div class="mobile-menu" id="mobileMenu">` with:
    ```html
    <nav class="nav" role="navigation" aria-label="Mobile navigation">
        <a href="#experience" class="nav__link" onclick="closeMobileMenu()">About</a>
        <a href="#services" class="nav__link" onclick="closeMobileMenu()">What I build</a>
        <a href="#contact" class="nav__link" onclick="closeMobileMenu()">Contact</a>
    </nav>
    ```

    DO NOT change: logo text, header__actions, social buttons, mobile-menu-toggle button, mobile-menu div wrapper, mobile menu JS functions, section id attributes anywhere in the file.

    POST-CHECK: Run `grep -n "href=\"#experience\"\|href=\"#services\"\|href=\"#contact\"\|href=\"#home\"" client/src/index.php` — count the occurrences before and after. The counts must match (no links were removed, only display text changed).
  </action>
  <verify>
    `grep -n "Services\|Events" client/src/index.php | grep "nav__link"` returns zero results.
    `grep -n "About\|What I build\|Contact" client/src/index.php | grep "nav__link"` returns 6 results (3 desktop + 3 mobile).
    `grep -c "href=\"#experience\"" client/src/index.php` returns 2 (desktop + mobile).
  </verify>
  <done>Desktop nav and mobile nav both show "About", "What I build", "Contact" with href targets #experience, #services, #contact. No "Services" or "Events" label anywhere in nav__link elements.</done>
</task>

<task type="auto">
  <name>Task 2: Fix footer background token in app.css</name>
  <files>client/src/assets/css/app.css</files>
  <action>
    In app.css, locate the `.footer` rule at line 490. The current line 491 reads:
    ```css
    background: var(--bg-surface);
    ```

    Change it to:
    ```css
    background: var(--bg-base);
    ```

    This is a one-line change inside the existing `.footer { }` block. Do not touch any other lines in the footer CSS block (lines 490–516).

    Rationale: The contact section uses `background: var(--bg-base)` (line 385 of app.css). The footer must match so the two sections blend seamlessly — no visible background step at the footer boundary.
  </action>
  <verify>
    `grep -n "\.footer {" client/src/assets/css/app.css` shows the block.
    `grep -A3 "\.footer {" client/src/assets/css/app.css | grep "background"` returns `background: var(--bg-base);`.
  </verify>
  <done>`.footer` CSS block uses `background: var(--bg-base)`. The old `var(--bg-surface)` value is gone from that rule.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Open index.php in a browser (or use `php -S localhost:8080 -t client/src/` if PHP CLI available). The nav bar must show three links: "About", "What I build", "Contact".
2. Clicking each nav link must scroll to the correct section (#experience, #services, #contact).
3. On mobile viewport (≤768px), the hamburger opens the mobile menu; tapping "About" scrolls to #experience and closes the menu.
4. Search the entire index.php for `href="#impressum"` and `href="#privacy"` — both must appear ONLY inside the `<footer>` element (lines 678–701), zero times inside `<header>` (lines 248–294).
5. `grep -n "bg-surface" client/src/assets/css/app.css | grep "\.footer"` returns zero results.
</verification>

<success_criteria>
- Nav labels: "About" (#experience), "What I build" (#services), "Contact" (#contact) — identical in desktop and mobile nav
- Impressum/Datenschutz: zero occurrences in header, confirmed present in footer
- Footer background: var(--bg-base) — matches contact section background
- No section IDs changed — all anchor scroll targets preserved
- No mobile menu JS modified
</success_criteria>

<output>
After completion, create `.planning/phases/03-navigation-and-layout-primitives/03-01-SUMMARY.md` using the summary template.
</output>
