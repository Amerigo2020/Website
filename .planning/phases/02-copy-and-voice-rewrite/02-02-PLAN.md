---
phase: 02-copy-and-voice-rewrite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/index.php
autonomous: true

must_haves:
  truths:
    - "The experience section h2 reads 'Background & Proof'"
    - "A <p> element containing the 4-sentence origin story exists immediately after that h2 and before the first cards-grid"
    - "grep -n 'Auszeichnungen\|Akademischer\|Agentur.*Industrie\|Netzwerk.*Mitglied' index.php returns zero matches"
    - "grep -n 'Unsere Erfahrung' index.php returns zero matches"
    - "Both instances of 'B.Sc. Information Systems' are replaced with 'Business Informatics'"
    - "The contact section h2 reads 'Start a conversation'"
    - "The contact intro paragraph reads 'Tell me about your project. I reply to every inquiry within 24 hours.'"
  artifacts:
    - path: "client/src/index.php"
      provides: "Origin story paragraph, English section headers, response-time commitment"
      contains: "Background & Proof"
  key_links:
    - from: "experience section h2"
      to: "origin story paragraph"
      via: "immediately adjacent sibling element"
      pattern: "Background & Proof.*I grew up"
    - from: "contact section h2"
      to: "response time commitment"
      via: "paragraph text"
      pattern: "Tell me about your project.*24 hours"
---

<objective>
Write and insert the positioning-anchored copy blocks: the origin story paragraph (CP-03) under the experience section heading, plus the contact section CTA and response-time commitment (CP-05). Replace all remaining German section and sub-section headings throughout the experience section.

Purpose: Satisfy CP-03 (origin story connecting family business, TUM, and present capability) and CP-05 (response time commitment visible near the contact form). Also satisfy CP-01 for the experience and contact sections specifically.
Output: index.php with English section headers, a 4-sentence origin story paragraph, corrected degree label in both locations, and a clear response-time commitment in the contact section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-copy-and-voice-rewrite/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace experience section headers and insert origin story paragraph</name>
  <files>client/src/index.php</files>
  <action>
Make the following targeted replacements in the experience section (around lines 375–558). Work top to bottom.

**Lines 375–376 — Experience section h2 and intro paragraph:**

Replace:
```html
<h2>Auszeichnungen & Expertise</h2>
<p>Unsere Erfahrung aus branchenübergreifenden Projekten, Hackathons und Auszeichnungen.</p>
```

With — note the existing `<p>` is replaced AND an origin story paragraph replaces it:
```html
<h2>Background & Proof</h2>
<p>I grew up helping run my family's IT services company, Jörg Velletti EDV Service — which meant debugging production systems long before I enrolled at university. At TUM studying Business Informatics, I developed the systems-thinking framing that connects technical decisions to business outcomes. Today I work as a student at EY in Transfer Pricing and compete in hackathons to keep shipping under pressure. I've won at the Enactus Germany Worldcup (Bangkok 2025) and placed top 3 at the MSG Hackathon.</p>
```

The `<div class="cards-grid"` immediately following this block is unchanged.

**Line 512 — "Akademischer Hintergrund" sub-section heading:**

Replace:
```html
<h3 style="margin-top: var(--spacing-lg); margin-bottom: var(--spacing-md);">Akademischer Hintergrund
</h3>
```
With:
```html
<h3 style="margin-top: var(--spacing-lg); margin-bottom: var(--spacing-md);">Education</h3>
```

**Line 518 — TUM degree chip — first instance of "B.Sc. Information Systems":**

Replace:
```html
<span class="chip">B.Sc. Information Systems</span>
```
With:
```html
<span class="chip">Business Informatics</span>
```

**Lines 531–532 — "Agentur- & Industrieerfahrung" sub-section heading:**

Replace:
```html
<h3 style="margin-top: var(--spacing-lg); margin-bottom: var(--spacing-md);">Agentur- &
    Industrieerfahrung
</h3>
```
With:
```html
<h3 style="margin-top: var(--spacing-lg); margin-bottom: var(--spacing-md);">Industry Experience</h3>
```

**Lines 558–559 — "Netzwerk & Mitgliedschaften" sub-section heading:**

Replace:
```html
<h3 style="margin-top: var(--spacing-lg); margin-bottom: var(--spacing-md);">Netzwerk & Mitgliedschaften
</h3>
```
With:
```html
<h3 style="margin-top: var(--spacing-lg); margin-bottom: var(--spacing-md);">Networks & Memberships</h3>
```
  </action>
  <verify>
```bash
grep -n 'Auszeichnungen\|Akademischer\|Industrieerfahrung\|Mitgliedschaften\|Unsere Erfahrung' client/src/index.php
grep -n 'Background & Proof\|I grew up helping\|Education\|Industry Experience\|Networks & Memberships' client/src/index.php
grep -n 'Information Systems' client/src/index.php
```
First grep: zero matches. Second grep: all five strings found. Third grep: zero matches (degree is now "Business Informatics" everywhere).
  </verify>
  <done>
Experience section h2 is "Background & Proof". The origin story paragraph (4 sentences, starts "I grew up helping run my family's IT services company") appears immediately after the h2 and before the first cards-grid. Sub-section h3s read "Education", "Industry Experience", "Networks & Memberships". The TUM chip reads "Business Informatics".
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite contact section CTA and LinkedIn fallback degree string</name>
  <files>client/src/index.php</files>
  <action>
Continue in index.php.

**Lines 603–604 — Contact section h2 and intro paragraph:**

Replace:
```html
<h2>Projektanfrage starten</h2>
<p>Bereit für das nächste Projekt? Schreiben Sie uns und wir melden uns zeitnah bei Ihnen.</p>
```
With:
```html
<h2>Start a conversation</h2>
<p>Tell me about your project. I reply to every inquiry within 24 hours.</p>
```

The response-time commitment is embedded in the single paragraph ("I reply to every inquiry within 24 hours"). No additional structural elements needed here — one `<p>` is sufficient per the research recommendation.

**Line 757 — LinkedIn fallback degree string (second instance of "B.Sc. Information Systems"):**

Replace:
```html
<p>B.Sc. Information Systems (Semester 6), Technische Universität München</p>
```
With:
```html
<p>Business Informatics, Technische Universität München (TUM)</p>
```

The semester number is intentionally omitted — it would go stale. The degree name "Business Informatics" now matches the experience card chip from Task 1. Do not alter any surrounding div or anchor tags on lines 753–764.
  </action>
  <verify>
```bash
grep -n 'Projektanfrage starten\|Bereit für\|wir melden uns\|Schreiben Sie' client/src/index.php
grep -n 'Start a conversation\|Tell me about your project\|24 hours' client/src/index.php
grep -n 'Information Systems\|Semester 6' client/src/index.php
```
First grep: zero matches. Second grep: all three strings found. Third grep: zero matches.
  </verify>
  <done>
Contact section h2 is "Start a conversation". The contact intro paragraph is "Tell me about your project. I reply to every inquiry within 24 hours." — the response-time commitment is visible, one sentence from the form. The LinkedIn fallback reads "Business Informatics, Technische Universität München (TUM)" with no semester number.
  </done>
</task>

</tasks>

<verification>
Run the complete experience + contact section sweep:

```bash
# All remaining German heading strings
grep -n 'Auszeichnungen\|Akademischer\|Agentur.*Industrie\|Netzwerk.*Mitglied\|Projektanfrage\|Bereit für' client/src/index.php

# Origin story present
grep -n 'I grew up helping run' client/src/index.php

# Response time commitment present
grep -n '24 hours' client/src/index.php

# Degree consistency — both instances should now say Business Informatics
grep -n 'Information Systems\|Business Informatics' client/src/index.php
```

Expected: first grep returns zero. "I grew up helping run" found once. "24 hours" found once. "Business Informatics" found at two locations (experience card chip and LinkedIn fallback); "Information Systems" found zero times.
</verification>

<success_criteria>
- Experience section h2 is "Background & Proof"
- Origin story paragraph (4 sentences, starting "I grew up helping run my family's IT services company") is the first element after the h2, before any cards-grid
- Experience sub-section h3s read "Education", "Industry Experience", "Networks & Memberships"
- TUM chip in education cards-grid reads "Business Informatics"
- Contact section h2 is "Start a conversation"
- Contact intro paragraph is "Tell me about your project. I reply to every inquiry within 24 hours."
- LinkedIn fallback p reads "Business Informatics, Technische Universität München (TUM)"
- "Information Systems" appears zero times in index.php
- "Semester 6" appears zero times in index.php
</success_criteria>

<output>
After completion, create `.planning/phases/02-copy-and-voice-rewrite/02-02-SUMMARY.md` following the summary template.
</output>
